"use strict";function voyage(evt, categorie) {    let tabcontent, tablinks;    tabcontent = document.getElementsByClassName("tabcontent");    for (let i = 0; i < tabcontent.length; i++) {        tabcontent[i].style.display = "none";    }    tablinks = document.getElementsByClassName("tablinks");    for (let i = 0; i < tablinks.length; i++) {        tablinks[i].className = tablinks[i].className.replace("active", "");    }    document.getElementById(categorie).style.display = "block";    evt.Target.className += "active";}$(function(){    let ul_forfaits = $(".forfaitslist").children("ul");    console.log("ul_forfaits", ul_forfaits);    for (let i = 0; i < forfaits_data.length; i++) {        let forfait = forfaits_data[i];        let ulf; // Le ul ou doit aller le forfait        switch (forfait.categorie) {            case 0:                ulf = ul_forfaits.eq(0);                break;            case 1:                ulf = ul_forfaits.eq(1);                break;            case 2:                ulf = ul_forfaits.eq(2);                break;            case 3:                ulf = ul_forfaits.eq(3);                break;            default:                // Numéro de catégorie inconnu        }        console.log("ul du forfait:", ulf);        // Créer un li, y mettre un hyperlien et le remplir        $("<li>")            .appendTo(ulf)            .append(`<a href="reservation.html?forfait=${i}"></a>`)            .children("a")            .append(`<h2>${forfait.nom}</h2>`)            .append(`<img src="images/${forfait.img_catalogue}" alt="${forfait.photo_alt}" />`);    }});